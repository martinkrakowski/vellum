// @generated out-port stub — edit freely
/**
 * ExternalServiceClientPort defines the contract for external ExternalServiceClient operations.
 *
 * This is an outbound port in the Hexagonal Architecture pattern.
 * Implement this interface in your infrastructure adapter.
 */
// TODO(phase-4.2): superseded by ExportPort (GLTFExporter primary, AuditTrail JSON + screenshot fallback).
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ExternalServiceClientPort {
  /**
   * TODO: Define your port methods here
   * Example:
   * fetch(id: string): Promise<Result<ExternalServiceClientData, Error>>;
   */
}
