# MCP server (stdio base)
# Advertised to clients; defaults to the project name.
MCP_SERVER_NAME=vellum
# Transport selector. The stdio base supports "stdio"; the mcp-server-http addon
# adds "streamable-http".
MCP_TRANSPORT=stdio
